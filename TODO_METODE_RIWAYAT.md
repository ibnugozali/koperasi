# TODO: Tambah Kolom Metode di /ketua/riwayat

## Langkah-langkah:
- [ ] 1. Update `repository/transaksi_repository.go` — tambahkan kolom metode di query `GetAllRiwayat()`
- [ ] 2. Update `templates/ketua/ketua_riwayat_transaksi.html` — tambahkan kolom Metode di tabel
- [ ] 3. Build & test aplikasi

